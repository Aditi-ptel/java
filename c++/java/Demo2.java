class Demo2 
{
    public static void main (String args[])
    {
        String str = "  Hello    World"; 

        // trim function removes excess space from the input
        System.out.println(str.trim()); 

        // length function counts the no. of elements, alphabets, numbers, also space in a string.
        System.out.println (str.length());
    }
}